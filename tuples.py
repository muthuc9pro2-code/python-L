trip_summary=("uberGo", "chennai", "airport", 450.00, "completed")
print(trip_summary)

print(trip_summary[1])

for item in trip_summary:
    print(item)

# tuples is just like List but it is immutable and it is defined by using () instead of [] and it is used to store multiple items in a single variable and it is ordered and it allows duplicate values and it is indexed and it is unchangeable and it is faster than list because of its immutability       

# list is dynamic but tuple is static


# dictionaries 

student = {
    "name": "Muthu",
    "age": 21,
    "city": "Chennai"
}

print(student["name"])

# dictionaries is just like object in js 

'''
 js object example:

const student = {
    name: "Muthu",
    age: 22,
    city: "Chennai"
}

console.log(student.name);
'''
